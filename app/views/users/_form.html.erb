<%= simple_form_for(@user) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :age %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
    <%= link_to_back %>
  </div>
<% end %>
